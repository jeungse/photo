 // 섹션별 스클롩 탑값 알아보기
 let sec1ScrTop = $('#section1').offset().top; // 섹션1의 맨위에서 탑값까지 위치
 let sec2ScrTop = $('#section2').offset().top; // 섹션2의 맨위에서 탑값까지 위치
 let sec3ScrTop = $('#section3').offset().top; // 섹션3의 맨위에서 탑값까지 위치
 let sec4ScrTop = $('#section4').offset().top; // 섹션4의 맨위에서 탑값까지 위치
 let sec5ScrTop = $('#section5').offset().top; // 섹션5의 맨위에서 탑값까지 위치
 let sec6ScrTop = $('#section6').offset().top; // 섹션6의 맨위에서 탑값까지 위치
 let sec7ScrTop = $('#section7').offset().top; // 섹션7의 맨위에서 탑값까지 위치
 let sec8ScrTop = $('#section8').offset().top; // 섹션8의 맨위에서 탑값까지 위치
 let sec9ScrTop = $('#section9').offset().top; // 섹션9의 맨위에서 탑값까지 위치
 let sec10ScrTop = $('#section10').offset().top; // 섹션10의 맨위에서 탑값까지 위치
 let sec11ScrTop = $('#section11').offset().top; // 섹션11의 맨위에서 탑값까지 위치
 let sec12ScrTop = $('#section12').offset().top; // 섹션12의 맨위에서 탑값까지 위치
 let sec13ScrTop = $('#section13').offset().top; // 섹션13의 맨위에서 탑값까지 위치
 let sec14ScrTop = $('#section14').offset().top; // 섹션14의 맨위에서 탑값까지 위치
 let sec15ScrTop = $('#section15').offset().top; // 섹션15의 맨위에서 탑값까지 위치
 let footerScrTop= $('#footer').offset().top; // 섹션15의 맨위에서 탑값까지 위치


 console.log(`sec1ScrTop   ${sec1ScrTop }`);
 console.log(`sec2ScrTop   ${sec2ScrTop }`);
 console.log(`sec3ScrTop   ${sec3ScrTop }`);
 console.log(`sec4ScrTop   ${sec4ScrTop }`);
 console.log(`sec5ScrTop   ${sec5ScrTop }`);
 console.log(`sec6ScrTop   ${sec6ScrTop }`);
 console.log(`sec7ScrTop   ${sec7ScrTop }`);
 console.log(`sec8ScrTop   ${sec8ScrTop }`);
 console.log(`sec9ScrTop   ${sec9ScrTop }`);
 console.log(`sec10ScrTop  ${sec10ScrTop}`);
 console.log(`sec11ScrTop  ${sec11ScrTop}`);
 console.log(`sec12ScrTop  ${sec12ScrTop}`);
 console.log(`sec13ScrTop  ${sec13ScrTop}`);
 console.log(`sec14ScrTop  ${sec14ScrTop}`);
 console.log(`sec15ScrTop  ${sec15ScrTop}`);
 console.log(`footerScrTop  ${footerScrTop}`);

